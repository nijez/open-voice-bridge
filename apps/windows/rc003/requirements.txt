# Runtime dependencies for the Open Voice Bridge · RC003 Windows client.
#
# All of these are optional at import time within ovb_rc003 (every module
# that needs one guards the import and raises a clear "*Unavailable* error
# instead of failing to import) - this lets the package's cross-platform
# pure-logic modules and their tests run on macOS/Linux without any of
# these installed. They are required to actually run the app on Windows.

sounddevice==0.5.5
numpy==2.4.3

# WinRT projections for BLE GATT (Bluetooth Low Energy Generic Attribute
# Profile) access to the RC003's ATVV voice service.
winrt-runtime==3.2.1
winrt-Windows.Devices.Bluetooth==3.2.1
winrt-Windows.Devices.Bluetooth.GenericAttributeProfile==3.2.1
winrt-Windows.Devices.Enumeration==3.2.1
winrt-Windows.Storage.Streams==3.2.1

# Button input (raw_input_windows.py) and key injection (win32_input.py) use
# only the standard library's ctypes against user32.dll/kernel32.dll - no
# extra package needed. (The earlier hidapi-based adapter was removed after
# XRBM-014 review RETRY P1 #5; see ATTRIBUTION.md.)
